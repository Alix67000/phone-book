import React from "react";

const EditContact = () => {
  return (
    <div className="footer">
      <button>Add</button>
      <button>Delete</button>
    </div>
  );
};

export default EditContact;
